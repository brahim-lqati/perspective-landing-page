const asyncHandler = require("express-async-handler");
const Contact = require('../models/Contact');
const C
exports.store = (req, res) => {
    const {name, sujet, email, content} = req.body;

    
}